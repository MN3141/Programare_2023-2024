name: Shell test
on: 
    push:
        branches:
          - 'main'
          - '1-jenkins-vs-githubaction-vs-bamboo'
jobs:
    date_print:
        runs-on: ubuntu-latest
        steps:
            - run: echo "You have made a push"
            - run: $(date)
            # This is a comment
            