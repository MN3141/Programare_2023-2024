name: Shell test
on: 
    push:
        branches:
          - 'main'
          - '1-jenkins-vs-githubaction-vs-bamboo'
jobs:
    date_print:
        runs-on: ubuntu-latest
        steps:
           - name : Running bash commands
             run: |
                echo "TEST"
                ls -la
